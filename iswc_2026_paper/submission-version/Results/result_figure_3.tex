\begin{figure*}[t]
    \centering

    \includegraphics[width=\textwidth]{Results/trend.png}

    \caption{
        Outcome matrix cells represent whether a query produced a non-empty result set (blue), an empty result set (yellow), an explicit error (pink), or was missing for that run (gray).
        The heatmap shows that failures are not confined to a small number of queries, but are widespread across the workload.
        For an interactive, expandable version of this figure see the \href{https://ecrum19.github.io/fed-survey-results/?mode=query&query=001&queryVariant=original}{results repository webpage}.
    }
    \label{fig:queriesTrend}
\end{figure*}